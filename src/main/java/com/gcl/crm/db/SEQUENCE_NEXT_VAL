DELIMITER $$
DROP FUNCTION IF EXISTS SEQUENCE_NEXT_VAL; $$
CREATE FUNCTION SEQUENCE_NEXT_VAL() RETURNS VARCHAR(15)
DETERMINISTIC
BEGIN
    SELECT CONCAT(DATE_FORMAT(SYSDATE(), '%y%m%d'), SUBSTR(uuid_short(), 11, 17)) INTO @SEQ_NO;
    RETURN @SEQ_NO;
END $$
DELIMITER ;